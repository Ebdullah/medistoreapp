  <nav class="navbar navbar-expand-lg navbar-dark bg-white"> <!-- Add left margin to the navbar -->
  <div class="container align-items-center">  
    <div>
      <% if policy(Notification).index? %>
        <button class="btn btn-lg btn-dark mb-1 px-4 py-2 d-flex align-items-center" onclick="window.location='<%= notifications_path %>'">
          Notifications
        </button>
      <% end %>
    </div>
  </div>
</nav>


<%# Style CSS for nav %>

<style>
.navbar-nav .nav-link:hover {
  color: #000 !important;
}
#navbarNavAltMarkup{
  margin-left: 200px;
}

</style>